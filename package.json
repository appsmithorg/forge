{
  "devDependencies": {
    "@rollup/plugin-node-resolve": "^15.2.3",
    "@rollup/plugin-terser": "^0.4.4",
    "rollup": "^2.79.1"
  },
  "scripts": {
    "build": "rollup -c",
    "readme.generate": "node readme.generate.js",
    "update": "npm run build & npm run readme.generate"
  }
}
